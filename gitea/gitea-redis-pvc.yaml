apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: ""
  volumeName: gitea-redis