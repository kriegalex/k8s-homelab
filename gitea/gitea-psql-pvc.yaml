apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-psql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: ""
  volumeName: gitea-psql